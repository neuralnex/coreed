// Coreed SDK - AI Deployment Platform for 0G Chain
// Export all public API

export { CoreedClient } from './CoreedClient';
export type { CoreedConfig, ModelMetadata, SpaceConfig, SleepStatus, SpaceHealth, ModelInfo, SpaceInfo } from './types';
export { DEFAULT_CONTRACT_ADDRESSES, DEFAULT_NETWORK } from './contracts';
