#!/usr/bin/env python3
"""
Coreed CLI - Entry point script for push-to-coreed

This script provides a convenient way to invoke push_to_coreed from the command line.
Install it with: pip install -e .

Or run directly: python -m cli.coreed_cli
"""

import sys
import os

# Add the parent directory to the path so we can import the cli module
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from cli.coreed_cli import main

if __name__ == "__main__":
    main()
